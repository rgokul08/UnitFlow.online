/** UnitFlow style: the transparency page is direct about scope, accuracy, and local data handling. */
import { CheckCircle2, Database, Gauge, ShieldCheck } from "lucide-react";
import { PageHeader } from "../components/PageHeader";
import { categories } from "../data/conversionData";

export default function About() { const totalUnits = categories.reduce((total, category) => total + category.units.length, 0); return <div className="page-wrapper page-enter about-page"><PageHeader eyebrow="ABOUT UNITFLOW" title="A calm tool for exact conversions." description="UnitFlow turns reliable measurement definitions into instant, reusable answers—without a login, an account, or unnecessary ceremony." /><section className="about-manifesto"><p>“When a calculation is routine, the interface should disappear. Keep the units, result, and next move in view.”</p><span>UNITFLOW DESIGN PRINCIPLE</span></section><section className="about-values"><article><Gauge size={20} /><h2>Designed for precision</h2><p>Category definitions live in one conversion engine. Temperature and fuel economy use scale-specific transformations; other categories convert through calibrated base factors.</p></article><article><Database size={20} /><h2>Local by default</h2><p>Conversions, history, favorites, and preferences are stored on your device through browser local storage.</p></article><article><ShieldCheck size={20} /><h2>Built for repeated work</h2><p>Search, keyboard-select units, share a conversion pair, choose output precision, and retain the pairings you use again.</p></article></section><section className="about-inventory"><div><p className="eyebrow">THE LIBRARY</p><h2>{categories.length} categories. {totalUnits} units. One workflow.</h2><p>From everyday length and volume to torque, data transfer, density, and fuel economy.</p></div><div className="about-category-list">{categories.map((category) => <span key={category.id}>{category.name}</span>)}</div></section></div>; }

